// Coding exercises — write-it-yourself practice with an in-browser test runner.
// Shape: lessonId -> { fn, prompt, starter, tests, pyFn, pyStarter, pyPrompt? }.
// Learners solve in Python (pyFn/pyStarter, run via Pyodide) or JavaScript
// (fn/starter, run via new Function) — the same tests judge both: return
// values are JSON-compared, so exercises return values (never print) —
// exactly the habit the Functions lesson preaches.
export const EXERCISES = {
  /* ── Module 0 — Foundations ── */
  m0l1: { fn: 'nextAge', prompt: 'Write nextAge(age): store age + 1 in a variable and return it. Your first box.',
    starter: 'function nextAge(age) {\n  // your code here\n}\n',
    pyFn: 'next_age', pyPrompt: 'Write next_age(age): store age + 1 in a variable and return it. Your first box.',
    pyStarter: 'def next_age(age):\n    # your code here\n    pass\n',
    tests: [{ args: [12], expected: 13 }, { args: [0], expected: 1 }, { args: [-1], expected: 0 }] },
  m0l2: { fn: 'parsePlusOne', prompt: 'parsePlusOne(s) receives a NUMBER AS TEXT like "41". Cast it to a number and return one more. ("41" -> 42, not "411"!)',
    starter: 'function parsePlusOne(s) {\n  // cast first, then add\n}\n',
    pyFn: 'parse_plus_one', pyPrompt: 'parse_plus_one(s) receives a NUMBER AS TEXT like "41". Cast it to a number and return one more. ("41" -> 42, not "411"!)',
    pyStarter: 'def parse_plus_one(s):\n    # cast first, then add\n    pass\n',
    tests: [{ args: ['41'], expected: 42 }, { args: ['0'], expected: 1 }, { args: ['-5'], expected: -4 }] },
  m0l3: { fn: 'greet', prompt: 'greet(name, age) returns the exact string: Hi NAME! Next year you will be AGE+1. — e.g. greet("Ada", 12) -> "Hi Ada! Next year you will be 13."',
    starter: 'function greet(name, age) {\n  // template strings: `Hi ${name}!`\n}\n',
    pyFn: 'greet',
    pyStarter: 'def greet(name, age):\n    # f-strings: f"Hi {name}!"\n    pass\n',
    tests: [{ args: ['Ada', 12], expected: 'Hi Ada! Next year you will be 13.' }, { args: ['Bo', 0], expected: 'Hi Bo! Next year you will be 1.' }] },
  m0l4: { fn: 'wrapHour', prompt: 'wrapHour(h) wraps any hour onto a 12-hour clock face using the remainder operator: 14 -> 2, 12 -> 0, 5 -> 5.',
    starter: 'function wrapHour(h) {\n  // one operator does it all\n}\n',
    pyFn: 'wrap_hour', pyPrompt: 'wrap_hour(h) wraps any hour onto a 12-hour clock face using the remainder operator: 14 -> 2, 12 -> 0, 5 -> 5.',
    pyStarter: 'def wrap_hour(h):\n    # one operator does it all\n    pass\n',
    tests: [{ args: [14], expected: 2 }, { args: [12], expected: 0 }, { args: [5], expected: 5 }, { args: [25], expected: 1 }] },
  m0l5: { fn: 'grade', prompt: 'grade(score) returns "great" for 80+, "pass" for 50-79, "retry" below 50. Mind the chain order!',
    starter: 'function grade(score) {\n  // if / else if / else — specific first\n}\n',
    pyFn: 'grade',
    pyStarter: 'def grade(score):\n    # if / elif / else — specific first\n    pass\n',
    tests: [{ args: [95], expected: 'great' }, { args: [80], expected: 'great' }, { args: [73], expected: 'pass' }, { args: [50], expected: 'pass' }, { args: [49], expected: 'retry' }] },
  m0l6: { fn: 'sumPrices', prompt: 'sumPrices(prices) loops over an array of numbers and returns their total. The accumulator pattern: init before, update inside, return after.',
    starter: 'function sumPrices(prices) {\n  let total = 0;\n  // for (const p of prices) ...\n  return total;\n}\n',
    pyFn: 'sum_prices', pyPrompt: 'sum_prices(prices) loops over a list of numbers and returns their total. The accumulator pattern: init before, update inside, return after.',
    pyStarter: 'def sum_prices(prices):\n    total = 0\n    # for p in prices: ...\n    return total\n',
    tests: [{ args: [[4, 7, 2]], expected: 13 }, { args: [[]], expected: 0 }, { args: [[10]], expected: 10 }] },
  m0l7: { fn: 'priceWithTax', prompt: 'priceWithTax(price, rate) returns price + price*rate, rounded to 2 decimals (Math.round(x * 100) / 100). Keep everything local; just return.',
    starter: 'function priceWithTax(price, rate) {\n  // compute tax locally, return the total\n}\n',
    pyFn: 'price_with_tax', pyPrompt: 'price_with_tax(price, rate) returns price + price*rate, rounded to 2 decimals (round(x * 100) / 100). Keep everything local; just return.',
    pyStarter: 'def price_with_tax(price, rate):\n    # compute tax locally, return the total\n    pass\n',
    tests: [{ args: [10, 0.08], expected: 10.8 }, { args: [100, 0.2], expected: 120 }, { args: [19.99, 0.1], expected: 21.99 }] },
  m0l8: { fn: 'sumOdds', prompt: 'Trace it, then write it: sumOdds(nums) returns the sum of only the ODD numbers. For [3,1,4] that is 4 — exactly the program you traced.',
    starter: 'function sumOdds(nums) {\n  let total = 0;\n  // loop + condition\n  return total;\n}\n',
    pyFn: 'sum_odds', pyPrompt: 'Trace it, then write it: sum_odds(nums) returns the sum of only the ODD numbers. For [3,1,4] that is 4 — exactly the program you traced.',
    pyStarter: 'def sum_odds(nums):\n    total = 0\n    # loop + condition\n    return total\n',
    tests: [{ args: [[3, 1, 4]], expected: 4 }, { args: [[2, 4, 6]], expected: 0 }, { args: [[5]], expected: 5 }] },
  m0l9: { fn: 'hasDuplicate', prompt: 'hasDuplicate(emails) returns true if any email appears twice. Chef A style please: ONE pass with a Set — no nested loops.',
    starter: 'function hasDuplicate(emails) {\n  const seen = new Set();\n  // one loop: seen.has? seen.add!\n}\n',
    pyFn: 'has_duplicate', pyPrompt: 'has_duplicate(emails) returns True if any email appears twice. Chef A style please: ONE pass with a set — no nested loops.',
    pyStarter: 'def has_duplicate(emails):\n    seen = set()\n    # one loop: in seen? seen.add!\n    pass\n',
    tests: [{ args: [['a@x.com', 'b@x.com', 'a@x.com']], expected: true }, { args: [['a@x.com', 'b@x.com']], expected: false }, { args: [[]], expected: false }] },

  /* ── Module 1 — OOP ── */
  m1l2: { fn: 'paintHouse', prompt: 'paintHouse(house, colour) returns a NEW house object with the new colour — the original must stay untouched (two houses, separate lives).',
    starter: 'function paintHouse(house, colour) {\n  // return { ...house, colour }\n}\n',
    pyFn: 'paint_house', pyPrompt: 'paint_house(house, colour) returns a NEW house dict with the new colour — the original must stay untouched (two houses, separate lives).',
    pyStarter: 'def paint_house(house, colour):\n    # return {**house, "colour": colour}\n    pass\n',
    tests: [{ args: [{ colour: 'white', windows: 4 }, 'ochre'], expected: { colour: 'ochre', windows: 4 } }, { args: [{ colour: 'blue' }, 'blue'], expected: { colour: 'blue' } }] },
  m1l4: { fn: 'makeHouse', prompt: 'A constructor by hand: makeHouse(colour, address) returns { colour, address, ready: true } — every field initialised, invariants holding.',
    starter: 'function makeHouse(colour, address) {\n  // build and return the object\n}\n',
    pyFn: 'make_house', pyPrompt: 'A constructor by hand: make_house(colour, address) returns {"colour": ..., "address": ..., "ready": True} — every field initialised, invariants holding.',
    pyStarter: 'def make_house(colour, address):\n    # build and return the dict\n    pass\n',
    tests: [{ args: ['white', '12 Oak St'], expected: { colour: 'white', address: '12 Oak St', ready: true } }] },

  /* ── Module 2 — Data Structures ── */
  m2l1: { fn: 'grabSlots', prompt: 'grabSlots(arr, i) returns [arr[i], last element] — two O(1) jumps, zero walking.',
    starter: 'function grabSlots(arr, i) {\n  // index arithmetic only\n}\n',
    pyFn: 'grab_slots', pyPrompt: 'grab_slots(arr, i) returns [arr[i], last element] — two O(1) jumps, zero walking. (Remember negative indexes.)',
    pyStarter: 'def grab_slots(arr, i):\n    # index arithmetic only\n    pass\n',
    tests: [{ args: [[12, 30, 21, 45, 9], 2], expected: [21, 9] }, { args: [[7], 0], expected: [7, 7] }] },
  m2l2: { fn: 'appendAll', prompt: 'appendAll(arr, items) returns a NEW array with items appended — the "grow by copying" a dynamic array does when full.',
    starter: 'function appendAll(arr, items) {\n  // spread or concat — no mutation\n}\n',
    pyFn: 'append_all', pyPrompt: 'append_all(arr, items) returns a NEW list with items appended — the "grow by copying" a dynamic array does when full.',
    pyStarter: 'def append_all(arr, items):\n    # + makes a new list — no mutation\n    pass\n',
    tests: [{ args: [[1, 2], [3, 4]], expected: [1, 2, 3, 4] }, { args: [[], [5]], expected: [5] }] },
  m2l3: { fn: 'listToArray', prompt: 'A node is { value, next } and the list ends at next: null. listToArray(head) follows the clues and returns the values in order.',
    starter: 'function listToArray(head) {\n  const out = [];\n  // while (node) { ...; node = node.next }\n  return out;\n}\n',
    pyFn: 'list_to_array', pyPrompt: 'A node is {"value": ..., "next": ...} and the list ends at next: None. list_to_array(head) follows the clues and returns the values in order.',
    pyStarter: 'def list_to_array(head):\n    out = []\n    # while node: ...; node = node["next"]\n    return out\n',
    tests: [{ args: [{ value: 7, next: { value: 3, next: { value: 9, next: null } } }], expected: [7, 3, 9] }, { args: [null], expected: [] }] },
  m2l4: { fn: 'countNodes', prompt: 'countNodes(head) walks a linked list ({ value, next }) and returns how many nodes it has. No arrays allowed — just the walk.',
    starter: 'function countNodes(head) {\n  let n = 0;\n  // walk and count\n  return n;\n}\n',
    pyFn: 'count_nodes', pyPrompt: 'count_nodes(head) walks a linked list ({"value", "next"} dicts) and returns how many nodes it has. No lists allowed — just the walk.',
    pyStarter: 'def count_nodes(head):\n    n = 0\n    # walk and count\n    return n\n',
    tests: [{ args: [{ value: 1, next: { value: 2, next: null } }], expected: 2 }, { args: [null], expected: 0 }] },
  m2l5: { fn: 'reverseWithStack', prompt: 'reverseWithStack(arr) pushes everything onto a stack, then pops it all — returning the reversed array. LIFO doing what LIFO does.',
    starter: 'function reverseWithStack(arr) {\n  const stack = [];\n  const out = [];\n  // push all, then pop all\n  return out;\n}\n',
    pyFn: 'reverse_with_stack', pyPrompt: 'reverse_with_stack(arr) pushes everything onto a stack (append), then pops it all — returning the reversed list. LIFO doing what LIFO does.',
    pyStarter: 'def reverse_with_stack(arr):\n    stack = []\n    out = []\n    # push all, then pop all\n    return out\n',
    tests: [{ args: [[1, 2, 3]], expected: [3, 2, 1] }, { args: [[]], expected: [] }] },
  m2l6: { fn: 'serve', prompt: 'serve(queue, k) serves the first k people FIFO-style: return [servedArray, remainingArray] without mutating the input.',
    starter: 'function serve(queue, k) {\n  // slice is your friend\n}\n',
    pyFn: 'serve', pyPrompt: 'serve(queue, k) serves the first k people FIFO-style: return [served_list, remaining_list] without mutating the input.',
    pyStarter: 'def serve(queue, k):\n    # slicing is your friend\n    pass\n',
    tests: [{ args: [['Ada', 'Bo', 'Cy'], 2], expected: [['Ada', 'Bo'], ['Cy']] }, { args: [['Ada'], 5], expected: [['Ada'], []] }] },
  m2l7: { fn: 'bucketOf', prompt: 'bucketOf(key, n) — the hash from the lesson: sum the character codes (str.charCodeAt(i)), take % n. Pure arithmetic, no searching.',
    starter: 'function bucketOf(key, n) {\n  let h = 0;\n  // sum char codes, then wrap\n}\n',
    pyFn: 'bucket_of', pyPrompt: 'bucket_of(key, n) — the hash from the lesson: sum the character codes (ord(ch)), take % n. Pure arithmetic, no searching.',
    pyStarter: 'def bucket_of(key, n):\n    h = 0\n    # sum ord(ch) for each ch, then wrap\n    pass\n',
    tests: [{ args: ['cat', 7], expected: 312 % 7 }, { args: ['dog', 7], expected: 314 % 7 }, { args: ['', 5], expected: 0 }] },
  m2l8: { fn: 'groupByBucket', prompt: 'groupByBucket(keys, n) hashes every key with the same char-code-sum % n and returns an array of n buckets (arrays) — collisions chain together.',
    starter: 'function groupByBucket(keys, n) {\n  const buckets = Array.from({ length: n }, () => []);\n  // hash each key, push into its bucket\n  return buckets;\n}\n',
    pyFn: 'group_by_bucket', pyPrompt: 'group_by_bucket(keys, n) hashes every key with the same ord-sum % n and returns a list of n buckets (lists) — collisions chain together.',
    pyStarter: 'def group_by_bucket(keys, n):\n    buckets = [[] for _ in range(n)]\n    # hash each key, append into its bucket\n    return buckets\n',
    tests: [{ args: [['a', 'b', 'h'], 7], expected: [['b'], [], [], [], [], [], ['a', 'h']] }] },
  m2l9: { fn: 'bstContains', prompt: 'bstContains(root, t): nodes are { v, left, right }. Use the BST rule — smaller goes left, bigger goes right — never search both sides.',
    starter: 'function bstContains(root, t) {\n  // walk down, choosing a side each step\n}\n',
    pyFn: 'bst_contains', pyPrompt: 'bst_contains(root, t): nodes are {"v", "left", "right"} dicts. Use the BST rule — smaller goes left, bigger goes right — never search both sides.',
    pyStarter: 'def bst_contains(root, t):\n    # walk down, choosing a side each step\n    pass\n',
    tests: [{ args: [{ v: 50, left: { v: 30, left: null, right: null }, right: { v: 70, left: null, right: null } }, 30], expected: true }, { args: [{ v: 50, left: null, right: null }, 99], expected: false }, { args: [null, 1], expected: false }] },
  m2l10: { fn: 'degreeOf', prompt: 'degreeOf(adj, node): adj is { A: ["B","C"], ... }. Return how many friends (edges) the node has — 0 if the node is unknown.',
    starter: 'function degreeOf(adj, node) {\n  // adjacency lookup + length\n}\n',
    pyFn: 'degree_of', pyPrompt: 'degree_of(adj, node): adj is {"A": ["B","C"], ...}. Return how many friends (edges) the node has — 0 if the node is unknown.',
    pyStarter: 'def degree_of(adj, node):\n    # adjacency lookup + len — .get saves the day\n    pass\n',
    tests: [{ args: [{ A: ['B', 'C'], B: ['A'] }, 'A'], expected: 2 }, { args: [{ A: [] }, 'Z'], expected: 0 }] },

  /* ── Module 3 — Algorithms (in the new learning order) ── */
  m3l6: { fn: 'linearSearch', prompt: 'linearSearch(arr, t) checks every drawer one by one: return the first index holding t, or -1. No sorting required — that is the point.',
    starter: 'function linearSearch(arr, t) {\n  // one loop, first match wins\n}\n',
    pyFn: 'linear_search', pyPrompt: 'linear_search(arr, t) checks every drawer one by one: return the first index holding t, or -1. No sorting required — that is the point.',
    pyStarter: 'def linear_search(arr, t):\n    # one loop, first match wins\n    pass\n',
    tests: [{ args: [[4, 2, 7, 2], 2], expected: 1 }, { args: [[1, 3], 9], expected: -1 }, { args: [[], 1], expected: -1 }] },
  m3l7: { fn: 'binarySearch', prompt: 'binarySearch(sorted, t): lo/hi bounds, probe the middle with Math.floor((lo+hi)/2), discard half each step. Return the index or -1. Watch the boundaries!',
    starter: 'function binarySearch(sorted, t) {\n  let lo = 0, hi = sorted.length - 1;\n  while (lo <= hi) {\n    // mid, compare, halve\n  }\n  return -1;\n}\n',
    pyFn: 'binary_search', pyPrompt: 'binary_search(sorted_arr, t): lo/hi bounds, probe the middle with (lo + hi) // 2, discard half each step. Return the index or -1. Watch the boundaries!',
    pyStarter: 'def binary_search(sorted_arr, t):\n    lo, hi = 0, len(sorted_arr) - 1\n    while lo <= hi:\n        # mid, compare, halve\n        pass\n    return -1\n',
    tests: [{ args: [[3, 7, 11, 18, 24, 31, 42], 18], expected: 3 }, { args: [[3, 7, 11], 3], expected: 0 }, { args: [[3, 7, 11], 12], expected: -1 }, { args: [[], 5], expected: -1 }] },
  m3l1: { fn: 'bubbleSort', prompt: 'bubbleSort(arr) returns a sorted copy using adjacent compare-and-swap passes. Bonus honesty: add the early exit when a pass makes no swaps.',
    starter: 'function bubbleSort(arr) {\n  const a = [...arr];\n  // nested loops + swap\n  return a;\n}\n',
    pyFn: 'bubble_sort', pyPrompt: 'bubble_sort(arr) returns a sorted copy using adjacent compare-and-swap passes. Bonus honesty: add the early exit when a pass makes no swaps. (No sorted() allowed!)',
    pyStarter: 'def bubble_sort(arr):\n    a = list(arr)\n    # nested loops + swap: a[j], a[j+1] = a[j+1], a[j]\n    return a\n',
    tests: [{ args: [[5, 3, 8, 1]], expected: [1, 3, 5, 8] }, { args: [[1, 2, 3]], expected: [1, 2, 3] }, { args: [[]], expected: [] }] },
  m3l2: { fn: 'selectionSort', prompt: 'selectionSort(arr): each pass, FIND the minimum of the unsorted region, then swap it into place. One swap per pass — that is its signature.',
    starter: 'function selectionSort(arr) {\n  const a = [...arr];\n  // find min index, swap to front\n  return a;\n}\n',
    pyFn: 'selection_sort', pyPrompt: 'selection_sort(arr): each pass, FIND the minimum of the unsorted region, then swap it into place. One swap per pass — that is its signature. (No sorted() allowed!)',
    pyStarter: 'def selection_sort(arr):\n    a = list(arr)\n    # find min index, swap to front\n    return a\n',
    tests: [{ args: [[64, 25, 12, 22]], expected: [12, 22, 25, 64] }, { args: [[2, 1]], expected: [1, 2] }] },
  m3l3: { fn: 'insertionSort', prompt: 'insertionSort(arr): grow a sorted left side; take each next card and shift bigger cards right until it fits. The way humans sort a hand of cards.',
    starter: 'function insertionSort(arr) {\n  const a = [...arr];\n  // for i from 1: shift & insert\n  return a;\n}\n',
    pyFn: 'insertion_sort', pyPrompt: 'insertion_sort(arr): grow a sorted left side; take each next card and shift bigger cards right until it fits. The way humans sort a hand of cards. (No sorted() allowed!)',
    pyStarter: 'def insertion_sort(arr):\n    a = list(arr)\n    # for i in range(1, len(a)): shift & insert\n    return a\n',
    tests: [{ args: [[5, 2, 4, 6, 1]], expected: [1, 2, 4, 5, 6] }, { args: [[1]], expected: [1] }] },
  m3l10: { fn: 'factorial', prompt: 'factorial(n) — recursively: the machine that presses its own button. Base case first (0! = 1), or the stack grows forever.',
    starter: 'function factorial(n) {\n  // base case, then n * factorial(n - 1)\n}\n',
    pyFn: 'factorial',
    pyStarter: 'def factorial(n):\n    # base case, then n * factorial(n - 1)\n    pass\n',
    tests: [{ args: [0], expected: 1 }, { args: [1], expected: 1 }, { args: [5], expected: 120 }] },
  m3l4: { fn: 'merge', prompt: 'The heart of merge sort: merge(a, b) zips two ALREADY-SORTED arrays into one sorted array with a two-pointer walk. No .sort() allowed!',
    starter: 'function merge(a, b) {\n  const out = [];\n  let i = 0, j = 0;\n  // compare fronts, take the smaller\n  return out;\n}\n',
    pyFn: 'merge', pyPrompt: 'The heart of merge sort: merge(a, b) zips two ALREADY-SORTED lists into one sorted list with a two-pointer walk. No sorted() allowed!',
    pyStarter: 'def merge(a, b):\n    out = []\n    i, j = 0, 0\n    # compare fronts, take the smaller\n    return out\n',
    tests: [{ args: [[1, 4, 9], [2, 3, 10]], expected: [1, 2, 3, 4, 9, 10] }, { args: [[], [1, 2]], expected: [1, 2] }, { args: [[5], []], expected: [5] }] },
  m3l5: { fn: 'partition', prompt: 'The heart of quicksort: partition(arr, pivot) returns [smaller, equalOrLarger] — two arrays, one pass, order preserved within each side.',
    starter: 'function partition(arr, pivot) {\n  // one loop, two buckets\n}\n',
    pyFn: 'partition', pyPrompt: 'The heart of quicksort: partition(arr, pivot) returns [smaller, equal_or_larger] — two lists, one pass, order preserved within each side.',
    pyStarter: 'def partition(arr, pivot):\n    # one loop, two buckets\n    pass\n',
    tests: [{ args: [[5, 9, 2, 7, 1], 5], expected: [[2, 1], [5, 9, 7]] }, { args: [[3, 3], 3], expected: [[], [3, 3]] }] },
  m3l8: { fn: 'bfsOrder', prompt: 'bfsOrder(adj, start): ripples with a QUEUE (shift from the front, push to the back). Mark seen on enqueue. Return the visit order.',
    starter: 'function bfsOrder(adj, start) {\n  const order = [];\n  const queue = [start];\n  const seen = new Set([start]);\n  // while queue: shift, record, enqueue unseen neighbours\n  return order;\n}\n',
    pyFn: 'bfs_order', pyPrompt: 'bfs_order(adj, start): ripples with a QUEUE (pop from the front, append to the back). Mark seen on enqueue. Return the visit order.',
    pyStarter: 'def bfs_order(adj, start):\n    order = []\n    queue = [start]\n    seen = {start}\n    # while queue: pop(0) (or use a deque), record, enqueue unseen neighbours\n    return order\n',
    tests: [{ args: [{ A: ['B', 'C'], B: ['D'], C: ['D'], D: [] }, 'A'], expected: ['A', 'B', 'C', 'D'] }, { args: [{ X: [] }, 'X'], expected: ['X'] }] },
  m3l9: { fn: 'dfsOrder', prompt: 'dfsOrder(adj, start): one corridor to the end — recurse into each unseen neighbour IN ARRAY ORDER before moving to the next. Return the visit order.',
    starter: 'function dfsOrder(adj, start) {\n  const order = [];\n  const seen = new Set();\n  function visit(node) {\n    // mark, record, recurse\n  }\n  visit(start);\n  return order;\n}\n',
    pyFn: 'dfs_order', pyPrompt: 'dfs_order(adj, start): one corridor to the end — recurse into each unseen neighbour IN LIST ORDER before moving to the next. Return the visit order.',
    pyStarter: 'def dfs_order(adj, start):\n    order = []\n    seen = set()\n    def visit(node):\n        # mark, record, recurse\n        pass\n    visit(start)\n    return order\n',
    tests: [{ args: [{ A: ['B', 'C'], B: ['D'], C: ['D'], D: [] }, 'A'], expected: ['A', 'B', 'D', 'C'] }, { args: [{ X: [] }, 'X'], expected: ['X'] }] },

  /* ── Module 4 — Big O ── */
  m4l1: { fn: 'pairCount', prompt: 'pairCount(n): how many comparisons does the "check every pair" chef make among n guests? Return the formula, not a loop: n·(n−1)/2.',
    starter: 'function pairCount(n) {\n  // arithmetic beats iteration\n}\n',
    pyFn: 'pair_count', pyPrompt: 'pair_count(n): how many comparisons does the "check every pair" chef make among n guests? Return the formula, not a loop: n·(n−1)//2.',
    pyStarter: 'def pair_count(n):\n    # arithmetic beats iteration — use // to stay an int\n    pass\n',
    tests: [{ args: [4], expected: 6 }, { args: [100], expected: 4950 }, { args: [1], expected: 0 }] },
  m4l8: { fn: 'firstDuplicate', prompt: 'Space-for-time trade: firstDuplicate(arr) returns the first value seen twice (or null). Use a Set — O(n) extra space buys O(n) time.',
    starter: 'function firstDuplicate(arr) {\n  const seen = new Set();\n  // one pass\n  return null;\n}\n',
    pyFn: 'first_duplicate', pyPrompt: 'Space-for-time trade: first_duplicate(arr) returns the first value seen twice (or None). Use a set — O(n) extra space buys O(n) time.',
    pyStarter: 'def first_duplicate(arr):\n    seen = set()\n    # one pass\n    return None\n',
    tests: [{ args: [[2, 1, 3, 5, 3, 2]], expected: 3 }, { args: [[1, 2, 3]], expected: null }] },
};
