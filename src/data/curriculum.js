// Curriculum data for the Zero to Hero bootcamp.
// Lesson shape: { id, t: title, a: analogy label, e: ELI5 body, k: Tech body,
//                 v: visualizer key | null, q: { q, o: [options], x: answerIndex, w: why } }
export const MODULES = [
  {
    id: 'm1', n: 'Module 1', name: 'OOP via Analogies',
    blurb: 'Objects are just nouns with skills. Learn the four pillars through houses, genetics and multi-tools.',
    days: [1, 11],
    lessons: [
      { id: 'm1l1', t: 'Classes as Blueprints', a: 'The architect\'s drawing', v: 'oop',
        e: 'A class is a drawing of a house, not a house. You can hold one drawing and build a thousand houses from it — every house gets the same rooms in the same places, but each one is its own building with its own furniture. Nobody lives in the drawing.',
        k: 'A class is a type definition: it declares the fields (state) and methods (behaviour) that its instances will have, plus how they are initialised. It allocates nothing on its own; instantiation allocates memory and binds the fields to that instance.',
        q: { q: 'You define class House. How many houses exist?', o: ['One', 'Zero — you only have the drawing', 'As many as the class has fields'], x: 1, w: 'Defining a class creates a template. Nothing is built until you instantiate it.' } },
      { id: 'm1l2', t: 'Objects as Houses', a: 'Built from the drawing', v: 'oop',
        e: 'An object is an actual house standing on an actual street. Two houses from the same drawing can be painted different colours and hold different families — changing the curtains in one does not change the other. Each object keeps its own copy of the details.',
        k: 'An object is an instance: a distinct allocation with its own field values, referenced by an identity. Mutating one instance\'s state does not affect another instance of the same class; only shared/static members are common to all.',
        q: { q: 'You paint house A blue. What colour is house B?', o: ['Blue', 'Whatever it already was', 'Undefined'], x: 1, w: 'Instance state is per-object. A and B hold separate values.' } },
      { id: 'm1l3', t: 'Attributes vs Methods', a: 'What it has vs what it does', v: 'oop',
        e: 'A house HAS things: a colour, four windows, an address. A house DOES things: the door opens, the lights switch on. Attributes are the nouns you can point at; methods are the verbs the house can perform.',
        k: 'Attributes (fields/properties) hold state; methods are functions bound to the instance that read or mutate that state. Methods receive the instance implicitly (this/self), which is how they reach the fields without being handed them.',
        q: { q: 'openDoor() is a…', o: ['Attribute', 'Method', 'Class'], x: 1, w: 'It performs an action, so it is behaviour — a method.' } },
      { id: 'm1l4', t: 'Constructors', a: 'The build crew\'s checklist', v: 'oop',
        e: 'Before anyone moves in, the crew runs a checklist: pour the foundation, set the address, pick the paint. The constructor is that checklist — it runs exactly once, the moment the house is built, and guarantees nothing is missing.',
        k: 'The constructor runs at instantiation to establish the object\'s invariants: assign required fields, validate arguments, acquire resources. Code that depends on a fully-initialised object should never run before the constructor returns.',
        q: { q: 'How often does a constructor run per object?', o: ['Every method call', 'Exactly once, at creation', 'Once per class'], x: 1, w: 'One run per instance, at the moment that instance is created.' } },
      { id: 'm1l5', t: 'Encapsulation', a: 'Walls and a doorbell', v: 'oop',
        e: 'You do not climb through a stranger\'s window to ask a question — you ring the doorbell. The house decides what to show you. Encapsulation means the messy insides are private, and the doorbell (a public method) is the only agreed way in.',
        k: 'Encapsulation restricts direct access to internal state and exposes a controlled interface. Private fields let the class enforce invariants and change its implementation without breaking callers, since callers only depend on the public surface.',
        q: { q: 'Why make a field private?', o: ['It runs faster', 'So the class controls how it changes', 'Private fields use less memory'], x: 1, w: 'Privacy is about control and safe change, not performance.' } },
      { id: 'm1l6', t: 'Inheritance as Genetics', a: 'Children of the blueprint', v: 'oop',
        e: 'A Townhouse is a House — it inherits the walls, the roof and the front door without redrawing them, then adds a shared side wall of its own. Like a child inheriting eye colour: you get the family traits for free, plus whatever is uniquely yours.',
        k: 'A subclass inherits the fields and methods of its superclass and may extend them. This models an is-a relationship; the subclass can be used anywhere the superclass is expected (the Liskov substitution principle).',
        q: { q: 'Townhouse extends House. Does Townhouse have House\'s openDoor()?', o: ['Yes, inherited', 'No, must redefine it', 'Only if declared abstract'], x: 0, w: 'Subclasses inherit the superclass\'s members automatically.' } },
      { id: 'm1l7', t: 'Method Overriding', a: 'The child who cooks differently', v: 'oop',
        e: 'Your family recipe says "boil the pasta". You keep the recipe name but do it your own way — you fry it instead. Same instruction from the outside, different behaviour inside. That is overriding.',
        k: 'Overriding replaces the superclass implementation of a method with the subclass\'s own, keeping the signature identical. Dispatch is dynamic: the runtime picks the implementation from the object\'s actual type, not the declared type.',
        q: { q: 'A House reference points to a Townhouse. Which openDoor() runs?', o: ['House\'s', 'Townhouse\'s', 'Both'], x: 1, w: 'Dynamic dispatch selects by the object\'s real type.' } },
      { id: 'm1l8', t: 'Polymorphism as a Multi-tool', a: 'One handle, many heads', v: 'oop',
        e: 'You shout "make a sound!" at a dog, a cat and a car. All three understand the instruction; you get a bark, a meow and a honk. You did not need to know what you were talking to — one instruction, many behaviours.',
        k: 'Polymorphism lets one interface serve many concrete types. Callers program against the abstraction, so adding a new implementation requires no change at the call site — the core mechanism behind extensible designs.',
        q: { q: 'The point of polymorphism is…', o: ['Less memory', 'Callers need not know the concrete type', 'Faster loops'], x: 1, w: 'It decouples the caller from the implementation.' } },
      { id: 'm1l9', t: 'Abstraction', a: 'The steering wheel', v: 'oop',
        e: 'You steer a car with a wheel. You never think about the rack, the pinion or the hydraulics — the wheel hides all of it and gives you one simple thing to turn. Good abstraction leaves you only the controls you actually need.',
        k: 'Abstraction exposes essential behaviour and hides implementation detail, usually via an interface or abstract base. It reduces coupling: the consumer depends on a contract, so the detail behind it can be swapped freely.',
        q: { q: 'Abstraction mainly reduces…', o: ['File size', 'What the caller must know', 'Number of classes'], x: 1, w: 'It shrinks the caller\'s required knowledge to a contract.' } },
      { id: 'm1l10', t: 'Composition over Inheritance', a: 'Lego, not a family tree', v: 'oop',
        e: 'A family tree is fixed — you cannot pick new parents later. Lego is not: you snap an engine on, swap it for a bigger one, pull it off. Building an object from smaller parts it OWNS is usually more flexible than being born into a long family line.',
        k: 'Composition builds behaviour by holding collaborators (has-a) rather than inheriting it (is-a). It avoids deep, rigid hierarchies and the fragile-base-class problem, and allows behaviour to be swapped at runtime by injecting a different collaborator.',
        q: { q: 'Car HAS an Engine is…', o: ['Inheritance', 'Composition', 'Overriding'], x: 1, w: 'A has-a relationship is composition.' } }
    ]
  },
  {
    id: 'm2', n: 'Module 2', name: 'Data Structures Visualized',
    blurb: 'Every structure is a way of arranging things in a room. Bookshelves, treasure hunts, cafeteria lines, org charts.',
    days: [12, 22],
    lessons: [
      { id: 'm2l1', t: 'Arrays as Bookshelves', a: 'Numbered slots in a row', v: 'array',
        e: 'An array is a bookshelf with numbered slots. Because every slot is the same width and they sit in a row, you can jump straight to slot 7 without touching slots 0 to 6 — you just walk to it. But squeezing a book into the middle means shoving everything to the right.',
        k: 'An array is a contiguous block of equal-size elements, so the address of index i is base + i × size — O(1) random access. Insertion or deletion at index i requires shifting the following elements, making it O(n).',
        q: { q: 'Reading arr[500] on a 1000-element array costs…', o: ['O(1)', 'O(n)', 'O(log n)'], x: 0, w: 'The address is computed arithmetically — one step, regardless of size.' } },
      { id: 'm2l2', t: 'Dynamic Arrays', a: 'Moving to a bigger shelf', v: 'array',
        e: 'Your shelf is full and another book arrives. You cannot stretch the shelf, so you buy one twice the size and carry every book across. It is a big effort — but you only do it occasionally, so averaged over many books it stays cheap.',
        k: 'A dynamic array holds a capacity larger than its length; on overflow it allocates a larger buffer (typically 2×) and copies. A single resize is O(n), but doubling makes the amortised cost of append O(1).',
        q: { q: 'Amortised cost of appending to a dynamic array?', o: ['O(1)', 'O(n)', 'O(n²)'], x: 0, w: 'Doubling spreads the copy cost across many cheap appends.' } },
      { id: 'm2l3', t: 'Linked Lists as Treasure Hunts', a: 'Each clue points to the next', v: 'list',
        e: 'A treasure hunt: the first clue tells you where the second clue is, the second points to the third. Nothing is numbered and nothing is in a row — to reach clue 5 you must physically follow clues 1 through 4. But slipping a new clue in is trivial: rewrite one clue to point at it.',
        k: 'A linked list stores each element in a node holding a value and a pointer to the next node. There is no index arithmetic, so access is O(n) traversal, but insertion or removal given a node reference is O(1) — only pointers are rewritten, nothing shifts.',
        q: { q: 'Inserting after a known node in a linked list costs…', o: ['O(1)', 'O(n)', 'O(log n)'], x: 0, w: 'You rewrite two pointers. No elements move.' } },
      { id: 'm2l4', t: 'Doubly Linked Lists', a: 'Footprints in both directions', v: 'list',
        e: 'Now each clue also says where you came from, so you can walk the hunt backwards. It costs a bit more paper on every clue, but you are never stuck facing one way.',
        k: 'Each node holds next and prev pointers, enabling O(1) removal given only the node (no predecessor search) and bidirectional traversal, at the cost of one extra pointer per node and more pointer updates per mutation.',
        q: { q: 'The extra prev pointer buys you…', o: ['O(1) random access', 'Backward traversal and O(1) removal by node', 'Less memory'], x: 1, w: 'Access is still O(n); what you gain is direction and cheap removal.' } },
      { id: 'm2l5', t: 'Stacks as Tray Piles', a: 'Last tray on, first tray off', v: 'stack',
        e: 'Clean trays stack up in the cafeteria. You put a tray on top, and the next person takes the top one — the tray at the bottom has been there since breakfast. You only ever touch the top. That is a stack.',
        k: 'A stack is LIFO: push and pop both operate at one end in O(1). It is the structure behind function call frames, undo histories and depth-first traversal — anywhere you must finish the most recent thing before returning to the previous one.',
        q: { q: 'Push 1, 2, 3 then pop. What comes out?', o: ['1', '3', '2'], x: 1, w: 'Last in, first out — 3 was most recent.' } },
      { id: 'm2l6', t: 'Queues as Lunch Lines', a: 'First in line eats first', v: 'queue',
        e: 'The lunch line is fair: whoever arrived first gets served first, and new people join at the back. Nobody cuts in. That is a queue — add at one end, remove from the other.',
        k: 'A queue is FIFO: enqueue at the tail, dequeue from the head, both O(1) with a linked list or ring buffer. It underlies breadth-first traversal, task scheduling and any producer/consumer buffer.',
        q: { q: 'Enqueue A, B, C then dequeue. What comes out?', o: ['A', 'C', 'B'], x: 0, w: 'First in, first out — A arrived first.' } },
      { id: 'm2l7', t: 'Hash Maps as Dictionaries', a: 'Straight to the right page', v: 'hash',
        e: 'To find "otter" in a dictionary you do not start at page 1 — the word itself tells you roughly where to go. A hash map does the same trick with maths: it turns the key into a page number, then looks only at that page.',
        k: 'A hash function maps a key to a bucket index, giving average O(1) insert and lookup. Performance depends on a good hash and a low load factor; the map resizes and rehashes as it fills to keep buckets short.',
        q: { q: 'Average lookup cost in a well-sized hash map?', o: ['O(1)', 'O(log n)', 'O(n)'], x: 0, w: 'The key computes its own location — one bucket to check.' } },
      { id: 'm2l8', t: 'Collisions', a: 'Two words on one page', v: 'hash',
        e: 'Sometimes two different words land on the same page. You do not panic — you write both on that page and read the short list to see which one you wanted. Collisions are normal; you just need the lists to stay short.',
        k: 'Collisions occur when distinct keys hash to the same bucket. Separate chaining stores a list per bucket; open addressing probes for the next free slot. Both degrade toward O(n) if the load factor rises, which is why maps resize.',
        q: { q: 'Two keys hash to bucket 3. The map…', o: ['Overwrites the first', 'Stores both and scans the bucket', 'Throws an error'], x: 1, w: 'Chaining keeps both; the short bucket scan disambiguates.' } },
      { id: 'm2l9', t: 'Trees as Org Charts', a: 'One boss, many reports', v: 'graph',
        e: 'A company chart: one person at the top, each person has reports below them, and nobody has two bosses. To find someone you start at the top and take the right branch down — you never search the whole company.',
        k: 'A tree is an acyclic connected graph with a root and a parent-child hierarchy. In a balanced binary search tree the ordering invariant lets you discard half the remaining nodes at each step, giving O(log n) search, insert and delete.',
        q: { q: 'Search in a balanced BST costs…', o: ['O(1)', 'O(log n)', 'O(n)'], x: 1, w: 'Each comparison discards half the tree.' } },
      { id: 'm2l10', t: 'Graphs as Friendship Maps', a: 'Everyone knows everyone', v: 'graph',
        e: 'A friendship map is messier than an org chart: friendship goes both ways, circles loop back on each other, and there is no single person at the top. A graph is just dots (people) joined by lines (relationships). And here is the family secret: the org chart from last lesson IS one of these — a tree is simply a graph that happens to have no loops and one person at the top.',
        k: 'A graph is a set of vertices and edges, directed or undirected, possibly cyclic. It is stored as an adjacency list (space O(V+E), good for sparse graphs) or an adjacency matrix (O(V²), constant-time edge lookup). A tree is exactly the special case: a connected, acyclic graph with a designated root — every tree is a graph; most graphs are not trees.',
        q: { q: 'A tree is…', o: ['Unrelated to graphs', 'A connected acyclic graph', 'A graph with a matrix'], x: 1, w: 'Every tree is a graph — just one with no cycles and a root.' } }
    ]
  },
  {
    id: 'm3', n: 'Module 3', name: 'Algorithms in Motion',
    blurb: 'Watch the work happen frame by frame. Compare, swap, split, merge, halve, traverse.',
    days: [23, 34],
    lessons: [
      { id: 'm3l1', t: 'Bubble Sort', a: 'Kids lining up by height', v: 'bubble',
        e: 'Walk down a line of kids and only ever compare two side by side. If the left one is taller, they trade places. One full sweep pushes the tallest kid all the way to the back — like a bubble floating up. Sweep again and again until nobody needs to move.',
        k: 'Bubble sort repeatedly traverses the array, swapping adjacent out-of-order pairs. Each pass places the largest unsorted element at its final index. Worst and average O(n²); best O(n) with an early-exit flag. Stable and in-place.',
        q: { q: 'What does one full pass of bubble sort guarantee?', o: ['The array is sorted', 'The largest unsorted element is in place', 'Half the array is sorted'], x: 1, w: 'Each sweep drags exactly one element to its final seat.' } },
      { id: 'm3l2', t: 'Selection Sort', a: 'Picking the shortest kid each time', v: 'selection',
        e: 'Scan the whole line and find the very shortest kid, then bring them to the front. Now scan the rest, find the shortest of those, put them second. You look a lot but you barely move anyone — one swap per round.',
        k: 'Selection sort finds the minimum of the unsorted region and swaps it into position i. Comparisons are always n(n−1)/2 — O(n²) regardless of input — but writes are only O(n), which matters when writes are expensive. Not stable in its usual form.',
        q: { q: 'How many swaps does selection sort make?', o: ['O(n)', 'O(n²)', 'O(log n)'], x: 0, w: 'One swap per round, n rounds — cheap on writes, costly on reads.' } },
      { id: 'm3l3', t: 'Insertion Sort', a: 'Sorting a hand of cards', v: 'insertion',
        e: 'You pick up cards one at a time and slide each into the right spot among the cards already in your hand. The left side of your hand is always sorted and grows by one each turn. If the cards arrive nearly sorted, you barely move anything.',
        k: 'Insertion sort grows a sorted prefix, shifting elements right to place each new key. O(n²) worst case but O(n) on nearly-sorted input and very low constant factors, which is why real sorts fall back to it for small subarrays. Stable and in-place.',
        q: { q: 'Insertion sort is fastest when the input is…', o: ['Reverse sorted', 'Nearly sorted', 'Random'], x: 1, w: 'Few inversions means almost no shifting — close to O(n).' } },
      { id: 'm3l4', t: 'Merge Sort', a: 'Two sorted lines zipped together', v: 'merge',
        e: 'Split the line in half, and again, until every group is one kid — a group of one is already sorted. Then zip pairs of sorted groups back together by repeatedly taking the shorter kid from the front of either group. Splitting is free; the zipping does the work.',
        k: 'Merge sort divides the array in half recursively and merges sorted halves in linear time. Depth log n × O(n) merge work gives O(n log n) in all cases, at the cost of O(n) auxiliary space. Stable, and the basis of external sorting.',
        q: { q: 'Merge sort\'s worst case is…', o: ['O(n²)', 'O(n log n)', 'O(n)'], x: 1, w: 'The recursion depth and merge cost do not depend on input order.' } },
      { id: 'm3l5', t: 'Quick Sort', a: 'Pick a kid, split the room', v: 'quick',
        e: 'Point at one kid — the pivot. Everyone shorter shuffles to their left, everyone taller to their right. That pivot is now in their final spot forever, and you repeat on each side. Fast when your pivot lands near the middle; slow if you keep picking the tallest.',
        k: 'Quicksort partitions around a pivot so that the pivot reaches its final index, then recurses on each side. Average O(n log n) with excellent cache behaviour and O(log n) stack space; worst case O(n²) on adversarial pivots, mitigated by randomised or median-of-three selection.',
        q: { q: 'After one partition, what is guaranteed?', o: ['Both halves are sorted', 'The pivot is in its final position', 'The array is sorted'], x: 1, w: 'Partitioning fixes exactly one element — the pivot.' } },
      { id: 'm3l6', t: 'Linear Search', a: 'Checking every drawer', v: 'linear',
        e: 'You lost your keys, so you open every drawer in order until you find them. It works on any mess — nothing needs to be tidy — but if the keys are in the last drawer you opened them all.',
        k: 'Linear search examines each element until a match is found. O(n) worst and average, O(1) best. It requires no ordering or preprocessing, which makes it the correct choice for small or unsorted data.',
        q: { q: 'Linear search requires the data to be…', o: ['Sorted', 'In any order', 'Hashed'], x: 1, w: 'It makes no assumptions — that is its advantage.' } },
      { id: 'm3l7', t: 'Binary Search', a: 'Guessing a number, halving each time', v: 'search',
        e: 'I picked a number between 1 and 100. You guess 50; I say "higher". Half the numbers just vanished. Guess 75, "lower" — half again. Seven guesses gets you any number out of a hundred, but only because the numbers are in order.',
        k: 'Binary search repeatedly compares the middle element of a sorted range and discards the half that cannot contain the target, giving O(log n) time and O(1) space. It requires sorted, randomly-accessible data — the precondition is the whole trick.',
        q: { q: 'Binary search on an unsorted array…', o: ['Still works, just slower', 'Gives wrong answers', 'Becomes O(n)'], x: 1, w: 'Discarding a half is only valid if order guarantees the target is not there.' } },
      { id: 'm3l8', t: 'Breadth-First Search', a: 'Ripples in a pond', v: 'bfs',
        e: 'Drop a stone in a pond. The ripple reaches everything one step away, then everything two steps away, then three. BFS explores a map the same way — all your friends first, then all their friends — which is why the first time it reaches a place, it got there by the shortest route.',
        k: 'BFS explores vertices in non-decreasing order of distance from the source using a queue, visiting each vertex and edge once for O(V+E). On unweighted graphs it yields shortest paths; the queue may hold O(V) vertices.',
        q: { q: 'BFS uses which structure?', o: ['Stack', 'Queue', 'Heap'], x: 1, w: 'FIFO order is what keeps the exploration level by level.' } },
      { id: 'm3l9', t: 'Depth-First Search', a: 'One corridor to the end', v: 'dfs',
        e: 'In a maze you pick a corridor and follow it as far as it goes. Dead end? Back up to the last junction and take the next corridor. DFS commits to a path completely before considering alternatives.',
        k: 'DFS follows each branch to exhaustion before backtracking, using an explicit stack or recursion, in O(V+E). It does not give shortest paths, but it is the basis of cycle detection, topological sorting and connected-component labelling.',
        q: { q: 'Which finds the shortest unweighted path?', o: ['DFS', 'BFS', 'Both'], x: 1, w: 'Only BFS explores in distance order.' } },
      { id: 'm3l10', t: 'Recursion', a: 'Russian dolls', v: null,
        e: 'To count nested dolls you open one and ask the same question of what is inside: "how many dolls in there?" Eventually you open a doll that is solid — that is the base case, and the answers add back up on the way out. A problem that contains a smaller copy of itself is a recursive problem.',
        k: 'A recursive function calls itself on a smaller input and terminates at a base case; each call consumes a stack frame, so depth costs O(depth) space. Missing or unreachable base cases cause stack overflow; tail calls or an explicit stack convert recursion to iteration.',
        q: { q: 'What stops a recursion?', o: ['The compiler', 'The base case', 'The stack size'], x: 1, w: 'A correct base case is the only intentional stopping condition.' } }
    ]
  },
  {
    id: 'm4', n: 'Module 4', name: 'Time & Space Complexity',
    blurb: 'Big O is not maths homework. It is the answer to: what happens when the data gets big?',
    days: [35, 45],
    lessons: [
      { id: 'm4l1', t: 'What Big O Measures', a: 'Cooking for 1 vs 100 guests', v: 'bigo',
        e: 'A recipe for one guest tells you nothing about a wedding. Big O ignores how long one step takes and asks only: if the guest list gets ten times bigger, does the work get ten times bigger, or a hundred times bigger? It is about the shape of the growth, not the stopwatch.',
        k: 'Big O gives an asymptotic upper bound on how a cost function grows with input size, discarding constants and lower-order terms. It compares algorithms independently of hardware; constants still matter in practice at small n.',
        q: { q: 'Big O describes…', o: ['Exact runtime in ms', 'How cost grows with input size', 'Memory used at n = 1'], x: 1, w: 'It is a growth rate, deliberately hardware-independent.' } },
      { id: 'm4l2', t: 'O(1) — Constant', a: 'A light switch', v: 'bigo',
        e: 'Flipping a light switch takes the same moment whether the house has one bulb or a thousand. The size of the problem simply does not enter into it.',
        k: 'O(1) means the operation cost is bounded by a constant regardless of n: array indexing, hash lookup, stack push. It may still be slow in absolute terms — constant does not mean fast, it means unaffected by size.',
        q: { q: 'Which is O(1)?', o: ['Summing an array', 'Reading arr[i]', 'Sorting an array'], x: 1, w: 'Index access is address arithmetic — one step, always.' } },
      { id: 'm4l3', t: 'O(n) — Linear', a: 'Shaking every hand', v: 'bigo',
        e: 'You greet every guest at the door with one handshake each. Twice the guests, twice the handshaking — a straight line. Perfectly reasonable, and often the best you can do if you genuinely must look at everything.',
        k: 'O(n) means cost grows in direct proportion to input size — a single pass such as summing, searching unsorted data, or copying. It is optimal for any problem that must inspect every element at least once.',
        q: { q: 'Summing 2,000 numbers vs 1,000 takes roughly…', o: ['The same time', 'Twice as long', 'Four times as long'], x: 1, w: 'Linear growth: double the input, double the work.' } },
      { id: 'm4l4', t: 'O(n²) — Quadratic', a: 'Everyone greets everyone', v: 'bigo',
        e: 'Now every guest must shake hands with every other guest. Ten guests is manageable; a hundred guests is not ten times worse, it is a hundred times worse. Nested loops over the same data feel fine in testing and fall over in production.',
        k: 'O(n²) arises from nested iteration over the input — bubble sort, naive pair comparison, adjacency-matrix scans. Growth is quadratic: 10× the input is 100× the work, which usually makes it unusable past a few thousand elements.',
        q: { q: 'Input goes from 100 to 1,000. An O(n²) algorithm does about…', o: ['10× the work', '100× the work', '1,000× the work'], x: 1, w: '10² = 100 — that is the trap.' } },
      { id: 'm4l5', t: 'O(log n) — Logarithmic', a: 'Halving the phone book', v: 'bigo',
        e: 'Tear the phone book in half and throw away the half that cannot contain your name. Do it again. A million names takes about twenty tears. Doubling the book adds a single extra step — this is the best kind of scaling there is.',
        k: 'O(log n) arises when each step discards a constant fraction of the remaining input: binary search, balanced tree operations, heap sift. Doubling n adds one operation, so it is effectively flat at any realistic scale.',
        q: { q: 'Doubling the input for an O(log n) algorithm adds…', o: ['Double the steps', 'About one step', 'No steps'], x: 1, w: 'Each doubling costs exactly one more halving.' } },
      { id: 'm4l6', t: 'O(n log n) — The Sorting Floor', a: 'Handshakes with a plan', v: 'bigo',
        e: 'You cannot sort a pile without looking at every item (that is the n), and the smartest arrangements halve the problem as they go (that is the log n). Together they are the realistic best for general sorting — much closer to a line than to a curve.',
        k: 'O(n log n) is the lower bound for comparison-based sorting and the complexity of merge sort, heapsort and practical quicksort. It sits close enough to linear that it is rarely the bottleneck.',
        q: { q: 'The best possible comparison sort is…', o: ['O(n)', 'O(n log n)', 'O(log n)'], x: 1, w: 'Comparison sorting cannot beat n log n in the general case.' } },
      { id: 'm4l7', t: 'Comparing the Curves', a: 'Four lines on one chart', v: 'bigo',
        e: 'Put all four growth shapes on one chart and drag the data size. At ten items they are almost indistinguishable — which is exactly why bad choices survive testing. Drag to a thousand and the quadratic curve leaves the chart entirely.',
        k: 'Asymptotic differences are invisible at small n and dominant at large n. Choose by the growth class for the expected scale, then optimise constants; profile before assuming the asymptotically better algorithm wins at your actual input size.',
        q: { q: 'At n = 10, an O(n²) algorithm is…', o: ['Obviously broken', 'Often indistinguishable from O(n)', 'Faster than O(log n)'], x: 1, w: 'Small inputs hide growth problems — that is the danger.' } },
      { id: 'm4l8', t: 'Space Complexity', a: 'Counter space, not cooking time', v: 'bigo',
        e: 'Some recipes need every bowl in the kitchen; others use one pan. Space complexity asks how much extra room the work needs as the data grows — and sometimes you happily spend memory to save time, or the reverse.',
        k: 'Space complexity counts auxiliary memory as a function of n, excluding the input. Merge sort needs O(n) auxiliary; quicksort O(log n) stack; in-place algorithms O(1). Recursion depth counts, which is why deep recursion overflows.',
        q: { q: '"In place" means auxiliary space of…', o: ['O(1)', 'O(n)', 'O(log n)'], x: 0, w: 'Constant extra space — the work happens in the original array.' } },
      { id: 'm4l9', t: 'Amortised Analysis', a: 'The occasional big shop', v: 'bigo',
        e: 'Most days you grab milk on the way home — quick. Once a month you do an enormous shop that eats an afternoon. Averaged across the month your shopping is cheap. Amortised cost is that average, and it is the honest number for things like growing an array.',
        k: 'Amortised analysis averages the cost of an operation over a worst-case sequence. Dynamic array append is amortised O(1) despite O(n) resizes because doubling makes resizes exponentially rare. Distinct from average-case, which averages over inputs.',
        q: { q: 'Amortised O(1) means…', o: ['Every call is O(1)', 'The average over a sequence is O(1)', 'It is O(1) on average inputs'], x: 1, w: 'Individual calls may be expensive; the sequence average is constant.' } },
      { id: 'm4l10', t: 'Choosing the Right Tool', a: 'The whole toolbox at once', v: 'bigo',
        e: 'You now know the shapes. Need instant lookup by name? Hash map. Need order and range queries? Sorted array or tree. Constantly inserting in the middle? Linked list. The skill is not memorising the table — it is asking "what will this do when the data is a hundred times bigger?"',
        k: 'Selection is a fit between access pattern and structure: hash map for keyed lookup, dynamic array for indexed iteration and cache locality, balanced tree for ordered queries, heap for repeated extreme extraction, adjacency list for sparse graphs. Decide by the dominant operation and expected n.',
        q: { q: 'You need lookup by ID and never need order. Use…', o: ['Sorted array', 'Hash map', 'Linked list'], x: 1, w: 'Keyed lookup with no ordering requirement is exactly a hash map.' } }
    ]
  }
];

export const ALL_LESSONS = MODULES.flatMap(m => m.lessons.map(l => ({ ...l, mid: m.id, mname: m.name, mn: m.n })));

// 2 hours a day: one lesson, its visualizer drill, the quiz, and a build task.
export const DAY_PLAN = [
  { k: 'Lesson + analogy', min: 35 },
  { k: 'Visualizer drill', min: 30 },
  { k: 'Quiz + code reading', min: 20 },
  { k: 'Build task from memory', min: 35 }
];
